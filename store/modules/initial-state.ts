// Export all initial-states
import { ProjectState } from '@definitions/Application.d';

// initial states
import config from './config/initial-state';

const state: ProjectState = {
  config,
};

export default state;
